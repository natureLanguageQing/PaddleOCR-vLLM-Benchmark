# PaddleOCR-VL 1.5 (vLLM) 本地服务化性能压测总结报告

> 📦 **开源代码与评测集库地址**：[https://github.com/natureLanguageQing/PaddleOCR-vLLM-Benchmark](https://github.com/natureLanguageQing/PaddleOCR-vLLM-Benchmark)

基于对本地部署的 **PaddleOCR-VL-1.5-0.9B** 视觉语言模型（搭配 vLLM 推理后端及 OpenAI 兼容 API）进行的多维度、高强度极限压力测试，现对该方案在**显存管理、并发处理、以及实际落地服务化**等方面的表现做出以下最终总结与评估。

---

## 0. 测试环境、硬件配置与测试参数

为保证性能指标测试结果的可复现性与商业评估价值，本次压测基于以下本地硬件底座与核心模型参数进行：

- **核心显卡 (GPU)**：NVIDIA GeForce RTX 4090 (24GB VRAM) - *决定性能指标的物理基石*
- **显卡驱动与计算环境**：Driver Version 596.36, CUDA 13.2
- **测试模型**：PaddleOCR-VL-1.5-0.9B
- **推理后端平台**：vLLM 本地服务化部署 (暴露 OpenAI 兼容 API，监听于 `http://127.0.0.1:8081`)
- **压测关键参数**：
  - 生成温度 (`temperature`)：`0` (保证 OCR 输出的确定性与准确度)
  - 最大输出长度 (`max-tokens`)：基准 `1024` (在专项长文本测试中拉升至 `8192`)
  - 核心性能评估指标：重点考核 **吞吐量 (RPS, Requests Per Second)**、**端到端平均延迟 (Latency)** 以及 **显存抗压稳定性 (防 OOM)**。

---

## 1. 核心维一：并发性能指标与吞吐量 (Concurrency & Throughput)

在压测过程中，我们逐步拔高了并发规模，从基础测试一路测到了极端的晚高峰堆积场景。模型引擎表现出了令人惊叹的**并发弹性**。

| 测试场景 | 请求总数 | 瞬间并发数 | 平均延迟 (Latency) | 吞吐量 (Throughput) | 结果 |
| :--- | :---: | :---: | :---: | :---: | :---: |
| **基准测试** | 20 | 8 | 1.57 秒 | 4.33 RPS | 正常 |
| **超大文件阻击** (10.5MB图) | 16 | 4 | 1.30 秒 | 2.89 RPS | 正常 |
| **高压长文本** (教育试卷) | 64 | 32 | 2.80 秒 | **10.57 RPS** | 正常 |
| **极限队列狂飙** | 128 | 64 | 3.19 秒 | **18.22 RPS** | 正常 |

> [!TIP]
> **结论分析：** 
> 随着并发数的成倍增加（从 8 增加到 64），单次请求的平均延迟并没有出现线性崩塌（仅仅从 1.5s 缓慢爬坡到了 3.1s），而吞吐量却呈现了指数级飙升（高达 18.2 份试卷/秒）。这证明了底层的 **vLLM Continuous Batching (连续批处理)** 机制在面对海量并发时，能够极其高效地榨干 GPU 的流处理器算力。

---

## 2. 核心维二：显存利用率与防爆机制 (VRAM Management)

大语言模型/多模态模型在服务化中最容易遇到的痛点就是显存溢出（OOM），我们对此进行了针对性极限测试。

- **超大 Payload 攻击**：通过发送高达 10.5 MB 充满随机噪点的 Base64 超清大图进行 I/O 轰炸，显存没有出现剧烈波动或宕机。
- **超大 上下文/Token 攻击**：我们将 `max-tokens` 拔高了 8 倍（至 8192），并输入了极其冗长复杂的英文 Prompt 提取指令。
- **极度密集的坐标预测**：对于密集排版的数学试卷，模型在输出文本的同时疯狂输出大量排版坐标 `<|LOC_xx|>` 导致长文本滞留。

> [!IMPORTANT]
> **结论分析：**
> 在瞬间 64 并发的高压、长图、长输出等极端混合叠加状态下，服务**未发生任何一次 OOM 或崩溃**（Success Rate: 100%）。这得益于 vLLM 底层的 **PagedAttention** 技术。它将 KV Cache 进行了内存分页管理，像操作系统管理内存一样彻底消除了显存碎片。这意味着该系统即使部署在显存吃紧的边缘计算节点或中端显卡上，也能保持极高的存活率。

---

## 3. 核心维三：服务化成熟度与场景落地 (Servitization)

一项技术的服务化能力不仅仅看速度，更看其接口的标准化、容错机制以及在真实业务场景中的泛化表现。

### A. 接口与生态整合
- 提供了完美的 **OpenAI 兼容 API** 接口（`/v1/chat/completions`）。
- 开发者可以使用市面上任何已有的 OpenAI SDK (Python/Java/Nodejs) 无缝接入，零成本完成系统集成，免去了重复造轮子的工程负担。

### B. 文档结构化解析 (教育场景验证)
- 面对结构极其复杂的高考理科数学试卷，PaddleOCR-VL 展现了强悍的排版解析和 OCR 能力。
- 所有的长文本输出都会伴随**精准的相对坐标 (Location Tokens)** 返回，天然支持下游研发用于“划词高亮”、“版面还原”或“试题入库分块”等高阶需求。
- 面对复杂 Prompt 的零样本 (Zero-Shot) 约束指令（如“提取 JSON”、“过滤无效选项”），多模态模型也能良好理解并执行预处理任务，减少了服务后端的正则校验压力。

---

## 🚀 最终总结与落地建议

总的来说，这套 **PaddleOCR-VL-1.5 + vLLM 架构** 已经具备了准工业级的生产力标准：

1. **可用作高吞吐中枢**：凭借 18+ RPS 的强悍吞吐量，完全可以作为一家中大型企业的发票、试卷、合同核心解析中枢。
2. **无需过高的硬件冗余**：稳定的显存调度证明，不需要为了防 OOM 而去租用顶配的 A100，利用多张中端卡进行负载均衡即可实现极高的性价比。
3. **系统部署与工程化演进架构图**：
   为了应对线上真实的不可控流量脉冲（例如超过 128 并发），建议在 vLLM 裸服务前置接入负载均衡与消息队列，形成如下高可用企业级架构：

   ```mermaid
   graph TD
       Client["业务客户端集群 / App端"] -->|"HTTP POST (图文数据)"| Nginx["Nginx 负载均衡网关"]
       Nginx -->|"限流 & 异步派发"| MQ["Kafka / Redis 缓冲队列"]
       
       subgraph "本地核心推理节点 (RTX 4090 集群)"
       MQ -.->|"消费提取"| Worker1["vLLM 实例 1 (PaddleOCR-VL 1.5)"]
       MQ -.->|"消费提取"| Worker2["vLLM 实例 N (PaddleOCR-VL 1.5)"]
       end
       
       Worker1 -->|"OpenAI API 格式回传"| Backend["业务中台逻辑层"]
       Worker2 -->|"OpenAI API 格式回传"| Backend
       Backend -->|"清洗、对齐与入库"| DB[("业务数据库 / 检索引擎")]
   ```

   - **防雪崩机制**：通过 `Redis/Kafka` 将超过单机系统瓶颈的溢出请求进行排队缓冲，确保底座大模型永远处于最佳计算区带 (Optimal Batch Size)。
   - **离线降本增效**：针对海量历史扫描卷宗、试卷归档，强烈建议绕开同步 HTTP 接口，单独编写基于 `vLLM LLM.generate()` 引擎的“离线大批量异步推流脚本”，夜间静默跑批，可将单张图片平均处理成本压至极限。
